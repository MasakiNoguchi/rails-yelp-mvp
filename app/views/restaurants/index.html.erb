<head>

</head>

<body>

  <div>
    <h1>My list of restaurants</h1>
    <% @restaurants.each do |restaurant| %>
<%= link_to restaurant.name, restaurant_path(restaurant.id), {class: "name"} %>
      <p><%= restaurant.address %></p>
    <% end %>
  </div>

  <div class="new">
    <button>test</button>
  </div>
</body>

